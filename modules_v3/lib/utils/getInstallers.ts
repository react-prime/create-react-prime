/**
 * @TODO
 * __REMINDER__ to store this as JSON because file structure changes with build
 */
export function getInstallers(): string[] {
  // fs.readdirSync();
  // Store as JSON

  return ['react-web', 'react-mobile'];
}
